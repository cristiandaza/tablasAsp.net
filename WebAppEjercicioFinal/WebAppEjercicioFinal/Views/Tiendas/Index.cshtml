@model IEnumerable<WebAppEjercicioFinal.Models.Tienda>

@{
    ViewData["Title"] = "Index";
}

<h1>Tienda</h1>

<p>
    <a asp-action="Create"><button class="btn btn-primary">Nuevo</button></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UbicacionProducto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.ProductoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UbicacionProducto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TiendaID" class="btn btn-success">Editar<i class="fas fa-edit"></i></a>
                <a asp-action="Details" asp-route-id="@item.TiendaID"class="btn btn-warning">Detalles<i class="fas fa-th-list"></i></a>
                <a asp-action="Delete" asp-route-id="@item.TiendaID"class="btn btn-danger">Eliminar<i class="fas fa-trash-alt"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
